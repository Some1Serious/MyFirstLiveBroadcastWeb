# ????
server:
  port: 8080
  servlet:
    session:
      timeout: 3600


# ?????
spring:
  jackson:
    generator:
      # 传输数字全部改成String, 这样避免Long精度丢失
      write-numbers-as-strings: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/livebroadcast?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false
    username: root
    password: Sxs12367
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl




